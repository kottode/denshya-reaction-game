export { C as ChildrenType } from '../ProtonJSX-CsiAR3VL.js';

type VNodeHandle = number;

declare const FragmentSymbol: symbol;

/**
 * Creates a flat vnode backed by the global VNodePool.
 *
 * The returned `VNodeHandle` is a number (index into the pool).
 * Children that are themselves valid `VNodeHandle` values are
 * automatically linked into a firstChild / nextSibling chain
 * for efficient tree iteration.
 */
declare function flatJSX(type: any, rawProps: Record<string, any> | null): VNodeHandle;

export { FragmentSymbol, type VNodeHandle, flatJSX as default, flatJSX };
